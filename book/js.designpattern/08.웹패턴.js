
(function () {
  // 1. 자바스크립트 전송
  // 1.1 파일결합
  var Westros;
  (function (Westros) {
    (function (Religion) {


    })(Westros.Religion || (Westros.Religion = {}));
    var Religion = Westros.Religion;
  })(Westros || (Westros = {}));
  (function (Westros) {
    var Tournament = (function () {
      function Tournament() {
      }

      return Tournament;
    });
  })();
  Westros.Tournament = Tournament;


  // 2. 플러그인
  // $(".dialog").dialog("open");
  // $(".dialog").dialog().open(); // 이게더 좋다?

  // 연쇄로 이루어짐



})();